import { cartSum } from 'store/helpers/cart-sum';
import { module, test } from 'qunit';

module('Unit | Helper | cart sum');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartSum([42]);
  assert.ok(result);
});
